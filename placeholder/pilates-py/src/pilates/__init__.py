"""Pilates — flex layout engine for terminal UIs.

This PyPI distribution is a brand-name placeholder. The Pilates project
is currently implemented in TypeScript and published under the
``@pilates`` scope on npm:

* ``@pilates/core``    — engine
* ``@pilates/render``  — out-of-box renderer
* ``@pilates/diff``    — incremental ANSI redraw
* ``@pilates/react``   — React reconciler

A Python port is not currently available. See
https://github.com/pilatesjs/pilates for the active project.
"""

__version__ = "0.0.1a0"
