# react-modal

`@pilates/react` port of `examples/modal`. Demonstrates absolute positioning: a centered confirmation dialog overlays a scrollable user list. The modal toggles after 1.5 s and the process exits after 3 s.

```bash
pnpm --filter @pilates-examples/react-modal dev
```
